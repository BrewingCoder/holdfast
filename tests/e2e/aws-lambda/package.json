{
	"name": "e2e-aws-lambda",
	"description": "aws-lambda",
	"version": "0.0.1",
	"private": true,
	"dependencies": {
		"@holdfast-io/node": "workspace:*"
	},
	"scripts": {
		"build": "sam build",
		"validate": "sam validate",
		"start": "sam local start-api"
	}
}
